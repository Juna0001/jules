# PairTrading_GSR.mq5 コードレビューレポート

作成された `PairTrading_GSR.mq5` のコードレビューを行い、ロジックの正確性、潜在的なリスク、および改善点を洗い出しました。

## 1. 評価概要

| 項目 | 評価 | コメント |
| :--- | :--- | :--- |
| **ロジックの正確性** | **高** | GSRの算出、平均回帰の判定、およびドル・ニュートラルなポジションサイジングは仕様通り正しく実装されています。 |
| **安全性** | **中** | 基本的なエラーチェック（シンボル有無、ゼロ除算）は実装されていますが、片側約定時の処理にリスクが残ります。 |
| **汎用性** | **高** | シンボル名やパラメータが入力変数化されており、多くのブローカー環境に対応可能です。 |

## 2. 検出された問題点とリスク

### 2.1. 片側約定（Execution Risk）
**詳細:**
ペアトレードでは2つの注文を同時に行う必要がありますが、プログラム上は順番に処理されます。
```cpp
if(trade.Sell(XAU...)) {
    if(!trade.Buy(XAG...)) {
        Print("Error opening Silver leg! Close Gold manually.");
    }
}
```
もし1つ目の注文（例：Gold売り）が成功し、2つ目の注文（例：Silver買い）が拒否された場合（証拠金不足、リクオート、通信エラーなど）、**片方のポジションだけが残る「片張り」状態**になります。
**対策:**
現在のコードではエラーログを出力して停止しますが、実運用では「2つ目が失敗したら即座に1つ目を決済する」というフェイルセーフロジックを追加することが推奨されます。

### 2.2. スプレッドコストの影響（Spread Impact）
**詳細:**
判定には「仲値（Mid Price）」を使用していますが、実際のエントリーは「Bid（売り）」と「Ask（買い）」で行われます。
*   **計算:** `Ratio = Mid_XAU / Mid_XAG`
*   **実行:** `Sell XAU (Bid) / Buy XAG (Ask)`
この乖離により、バックテスト上のパフォーマンスよりも実運用の成績が悪化する可能性があります（スリッページとは別のコスト要因）。
**対策:**
エントリー判定時に、スプレッド分を考慮した「実効レート」でのGSRを計算するか、`BB_Deviation` を少し広めに設定してコストを吸収する必要があります。

### 2.3. 計算負荷（Performance）
**詳細:**
`OnTick` 関数内で毎回 `CopyClose` と `CopyTime` を実行し、ループ処理で平均と標準偏差を計算しています。
ティック更新頻度が高いGold相場では、これがCPU負荷となる可能性があります。
**対策:**
「新しい足（New Bar）が確定した時」だけ統計データを再計算するようにロジックを変更することで、軽量化が可能です。

## 3. 実装上の良い点（Good Practices）

*   **データ同期（Data Synchronization）:**
    `CopyTime` を使用して、XAUの時刻に対応するXAGの終値を正確に取得しています。これにより、サーバーのデータ配信ズレによる誤った比率計算を防いでいます。
*   **リペイント防止:**
    移動平均の計算に「確定した足（インデックス1以降）」を使用しているため、リアルタイムでシグナルが点滅して消える（リペイント）現象を防いでいます。
*   **ポジション管理:**
    XAUとXAGの両方のポジション数をカウントし、不整合（片側だけ持っている状態）がある場合は新規エントリーを行わない安全策が取られています。

## 4. 結論

本EAは**プロトタイプとして十分に機能する品質**ですが、リアルマネーで運用する前には以下の追加実装を強く推奨します。

1.  **片側約定時の自動クローズ処理**（またはリトライ処理）
2.  **スプレッドフィルター**（スプレッドが広すぎる時はエントリーしない）
3.  **証拠金維持率の事前チェック**

現状のコードは、デモ口座でのフォワードテストや、戦略の有効性検証にはそのまま使用可能です。
